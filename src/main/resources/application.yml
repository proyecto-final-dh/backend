# spring:
#   datasource:
#     url: jdbc:mysql://db.ctd.academy:3306/${BACKEND_DB}?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC
#     username: ${BACKEND_USERNAME}
#     password: ${BACKEND_PASSWORD}

# bucketName: ${BUCKET_NAME}
# accessKey: ${S3_ACCESS_KEY_ID}
# secretKey: ${S3_SECRET_ACCESS_KEY}
# endpointUrl: https://${BUCKET_NAME}.s3.us-east-1.amazonaws.com
# jpa:
#     open-in-view: false
#     hibernate:
#       ddl-auto: none
#     properties:
#       hibernate:
#         dialect: org.hibernate.dialect.MySQL8Dialect

# bucket:
#   name: ${BUCKET_NAME}
#   accessKey: ${S3_ACCESS_KEY_ID}
#   secretKey: ${S3_SECRET_ACCESS_KEY}
#   url: https://${BUCKET_NAME}.s3.us-east-1.amazonaws.com

# resqpet:
#   app:
#     module-name: ResQPet
#     api-version: 1.0.0

# server:
#   # ssl:
#   #   key-alias: spring-https
#   #   key-store: classpath:backend.jks
#   #   key-store-type: JKS
#   #   key-password: passowrd
#   error:
#     include-message: always
#   port: 8080
spring:
  datasource:
    url: jdbc:mysql://db.ctd.academy:3306/${BACKEND_DB}?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC
    username: ${BACKEND_USERNAME}
    password: ${BACKEND_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

bucket:
  name: ${BUCKET_NAME}
  accessKey: ${S3_ACCESS_KEY_ID}
  secretKey: ${S3_SECRET_ACCESS_KEY}
  url: https://${BUCKET_NAME}.s3.us-east-1.amazonaws.com

resqpet:
  app:
    module-name: ResQPet
    api-version: 1.0.0

server:
  # ssl:
  #   key-alias: spring-https
  #   key-store: classpath:backend.p12
  #   key-store-type: PKCS12
  #   key-password: password
  error:
    include-message: always
  port: 8080
